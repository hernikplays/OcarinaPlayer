<Window x:Class="OcarinaPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OcarinaPlayer"
        mc:Ignorable="d"
        Title="Ocarina" MinWidth="800" MinHeight="450"
        Loaded="OnLoad"
        SizeToContent="WidthAndHeight"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="10"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}" Height="732" Width="1000">

    <Grid>
        <Grid VerticalAlignment="Top">
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="File" Height="27">
                    <MenuItem Header="Open file" Click="openFile"/>
                    <MenuItem Header="Open playlist" Click="openPL"/>
                    <MenuItem Header="Save playlist" Click="savePL"/>
                    <Separator />
                    <MenuItem Header="Exit" />
                </MenuItem>
            </Menu>
        </Grid>
        <Grid HorizontalAlignment="Right" Width="246">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <ListBox HorizontalAlignment="Left" Height="701" Margin="0,0,-77,0" VerticalAlignment="Top" Width="246" Name="Playlist" MouseLeftButtonUp="Playlist_Click">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Label Content="{Binding}" FontSize="20"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

        </Grid>
        <Grid VerticalAlignment="Bottom" Height="392" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Image HorizontalAlignment="Left" Height="254" Margin="10,35,0,0" VerticalAlignment="Top" Width="252" Grid.Column="0" Grid.Row="0" Name="albumArt" Source="noalbum.png"/>
            <Label Content="00:00" Width="96" HorizontalAlignment="Left" Margin="211,345,0,0" VerticalAlignment="Top" Grid.Column="0" Grid.Row="0" Name="cas" FontSize="23"/>


            <Label  Height="35" Margin="10,294,1062,63">
                <TextBlock FontSize="18" Name="artistSong" TextWrapping="Wrap"/>
            </Label>

            <Slider HorizontalAlignment="Left" Margin="293,353,0,0" VerticalAlignment="Top" Width="192" Grid.Column="0" Grid.Row="0" Name="volumeSlider" ValueChanged="volumeSlider_ValueChanged" Maximum="1" LargeChange="0.5" Value="1"/>

            <Grid Height="55" VerticalAlignment="Bottom" Width="196" HorizontalAlignment="Left" Margin="10,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="45"></ColumnDefinition>
                    <ColumnDefinition Width="45"></ColumnDefinition>
                    <ColumnDefinition Width="45"></ColumnDefinition>
                    <ColumnDefinition Width="45"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition ></RowDefinition>
                </Grid.RowDefinitions>
                <Button Padding="0" Grid.Column="0" Grid.Row="0"  Width="43" Click="btnPrev_Click" VerticalAlignment="Bottom" HorizontalAlignment="Left" Height="43" >
                    <materialDesign:PackIcon Kind="SkipPrevious" Foreground="Black" Width="40" Height="40" />
                </Button>
                <Button Padding="0" Grid.Column="1" Grid.Row="0"  Click="play" Height="43" Width="43" VerticalAlignment="Bottom" HorizontalAlignment="Left">
                    <materialDesign:PackIcon Kind="PlayArrow" Foreground="Black" Width="43" Height="40" Name="playButton" />
                </Button>
                <Button Padding="0" Grid.Column="2" Grid.Row="0" Width="43" Click="btnNext_Click" VerticalAlignment="Bottom" HorizontalAlignment="Left" Height="43" >
                    <materialDesign:PackIcon Kind="SkipNext" Foreground="Black" Width="40" Height="40" />
                </Button>
                <Button Padding="0" Grid.Column="3" Grid.Row="0" Width="43" Click="shuffle_Click" VerticalAlignment="Bottom" HorizontalAlignment="Left" Height="43" Name="shuffle">
                    <materialDesign:PackIcon Kind="Shuffle" Foreground="Black" Width="40" Height="40" />
                </Button>
            </Grid>

        </Grid>
    </Grid>
</Window>
